This folder contains **compiled results and visual diagnostics** generated during the model tuning and evaluation stages. These outputs were used to select optimal hyperparameters and compare model performance.

#### Contents
- **`tune_roc_tbl.rda`**
  A summary R object consolidating ROC-AUC metrics across all tuned models, used to **compare model effectiveness** and guide decisions for further refinement or ensemble modeling.

